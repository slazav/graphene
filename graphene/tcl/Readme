tcl packages

======================================================================
ROTA packages:

* xBLT -- extentions to TCL BLT library
* GPIB -- tcl interface to linux-gpib library
* ParseOptions
* Daemon
* Locking
* Prectime

======================================================================
* Graphene

Simple interface to Graphene database: open/close connection (local or
ssh), run command, get result.

======================================================================
* GrapheneMonitor

A monitor program for Graphene database. Simple modules are used for
reading data from variouse places. Data is read regularly and stored
in databases.
